using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class span : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject ref1;
    public GameObject ref2;
    public float green_destroyed=0f;
    public float red_destroyed=0f; 
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        // print(ref2.GetComponent<redmove>().speed);        
        // ref1=GameObject.Find("redball");
        // ref2=GameObject.Find("ball");   
        // print(ref1);
        // print(ref2);
        // print(ref2.GetComponent<move>().speed);
        // ref2.GetComponent<move>().speed=3f;
        // print(ref2.GetComponent<move>().speed); 
    }
}
